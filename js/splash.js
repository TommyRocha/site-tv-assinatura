(function($) {

"use strict";

$(document).ready(function(e) {
    
    var win 	= $(window);
	var win_h 	= $(window).height();
	var win_w 	= $(window).width();
	var win_h_h = $(window).height()/2;
	var win_w_h = $(window).width()/2;
	var web_width = $(".boxed").width();

	
});


})(jQuery);